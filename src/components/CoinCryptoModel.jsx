import { Flex, Typography, Divider, Tag } from 'antd';
export default function CoinInfoModal({ coin }) { if (!coin) return null; return <><Flex align="center" gap={12}><img src={coin.icon} alt={coin.name} className="coin-icon" style={{ width: 44, height: 44 }} /><div><Typography.Title level={3} style={{ margin: 0, color: '#fff' }}>{coin.name}</Typography.Title><span className="coin-symbol">{coin.symbol} · Rank #{coin.rank}</span></div></Flex><Divider /><div className="detail-grid"><div className="detail-stat"><span>Price</span><strong>{coin.price.toFixed(2)}$</strong></div><div className="detail-stat"><span>Market cap</span><strong>{(coin.marketCap / 1e9).toFixed(1)}B</strong></div><div className="detail-stat"><span>Volume 24h</span><strong>{(coin.volume / 1e9).toFixed(1)}B</strong></div></div><Typography.Paragraph style={{ color: '#aeb4cb' }}><Typography.Text strong style={{ color: '#eef2ff' }}>Performance</Typography.Text><br /><Tag color={coin.priceChange1h >= 0 ? 'green' : 'red'}>1h {coin.priceChange1h}%</Tag><Tag color={coin.priceChange1d >= 0 ? 'green' : 'red'}>1d {coin.priceChange1d}%</Tag><Tag color={coin.priceChange1w >= 0 ? 'green' : 'red'}>1w {coin.priceChange1w}%</Tag></Typography.Paragraph></>; }
